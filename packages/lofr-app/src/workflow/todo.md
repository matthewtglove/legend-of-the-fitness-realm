# todo

-   [] code as workflow (using lofr code)
    -   [x] {1 hour} setup workflow inside lofr project and load lofr code as a workflow
        -   [x] {5 mins} create the packages/workflow-server (bun) copy workflow-server from 3d-scene project
        -   [x] {5 mins} add react-flow to lofr
        -   [x] {5 mins} create empty workflow-editor folder in lofr-app/src/workflow/workflow-editor
        -   [x] {5 mins} open workflow-editor with empty react-flow
        -   [x] {5 mins} define hard coded lofr-workflow-loader that generates a workflow with hard coded nodes (prototype for workflow-as-code definition)
        -   [x] {5 mins} serve raw text files with the workflow-server
        -   [x] {10 mins} make a node to display raw text file from workflow-server (input relative path)
        -   [x] {10 mins} make a node to run a string function and display results from workflow-server (input relative path, function name)
        -   [x] {10 mins} make a node to display rendered js react component from workflow-server (input relative path, function name)
    -   [] {1 hour} live edit code in workflow editor
        -   [] {5 mins} make a node to list files from workflow-server and list in text box
        -   [] {5 mins} make a node to edit a text file from workflow-server (input relative path)
        -   [] {5 mins} copy the vscode text editor
        -   [] {5 mins} extend text node to use vscode text editor
        -   [] {10 mins} display a built react component from the workflow-server (using Bun.build, load from dist, no dependencies)
        -   [] {10 mins} display a hot reload react component from the workflow-server (using bun dev)
        -   [] {10 mins} edit the code for the hot reload react component while it updates immediately in the neighbor node
        -   [] {5 mins} add an output to the text node
        -   [] {5 mins} add an input to the react node and display the text from a connected text node (pull data from input node on refresh button)
    -   [] {1 hour} function call as node
        -   [] {15 mins} make function-runner node to load function type definition from workflow-server (input relative path, function name)
        -   [] {5 mins} display function type definition json from workflow-server (input relative path, function name)
        -   [] {15 mins} update function-runner inputs and outputs from function type definition
        -   [] {15 mins} connect inputs to function-runner and execute (display results in node as text)
        -   [] {10 mins} connect output to function-runner output
    -   [] {1 hour} function body as workflow
        -   [] {15 mins} make function-runner node to load simple function body ast from workflow-server (function call with single input and no output `callTextFun("fun")`)
        -   [] {15 mins} open function body workflow in editor and go back
        -   [] {15 mins} support function workflow input from parameter: `callTextFun(input)`
        -   [] {15 mins} support function workflow return: `return callTextFun(input)`
    -   [] {1 hour} variable node and assignment
        -   [] {15 mins} support const assignment: `const a = callTextFun(input)`
        -   [] {15 mins} make named variable node with single input: `a`
        -   [] {15 mins} make named variable node with single output: `const b = a; return b;`
        -   [] {5 mins} support let assignment: `let c = a; c = b;`
        -   [] {10 mins} allow named variable node to have multiple input slots (to allow multiple assigners): `a`
    -   [] {1 hour} function body (generic code block, just identify declared functions and called functions?)
    -   [] {1 hour} function body (generic code block, granular ast?)
